# 2026-09-21 — 3차 종합 검토 (Claude, Cowork)

- 범위: 09-16 2차 검토 이후 5일. 브랜치·커밋, 09-16 지적 반영 여부, 신규 기능(ERP·세무·노무·법인), 문서
- 기준: `feat/yahoo-fundamentals` HEAD는 여전히 `e15f070`(09-16). **09-16 이후 커밋 0개.** 미커밋 125파일(수정 54 +1,628/−835, 신규 71파일 4,920줄). `dev`=`de705f8`(09-08), `main`=09-02
- 실행: 리눅스 VM(Node 22)에서 core·api·web 타입체크 통과(미커밋 포함). 테스트·브라우저는 Grok 09-18 기록 인용

## 결론

**09-16 🔴 세 가지 중 된 것이 없다.** 그 사이 09-18 하루에 세션 17개로 ERP 업무홈·부가세·법인세·프리랜서 3.3%·직원 명부·건강보험 자격취득·공무원 호봉표·양도/증여/보유 세율표가 추가됐고(ADR 0032~0047), 전부 미커밋이며 **원장 저장 없이 화면·계산만** 있다. dev는 13일째 09-08이다. 지금 이 저장소의 상태를 한 문장으로 쓰면 "맥북 작업 폴더 하나에 3주치 결과물이 커밋 안 된 채 놓여 있고, 그 위에 매일 새 기능이 얹힌다"이다. 디스크 하나 잘못되면 09-08 이후가 전부 사라진다.

| # | 상태 | 09-16 지적 | 09-21 | 반영 (2026-09-23) |
|---|---|---|---|---|
| 1 | 🔴 **미반영** | dev fast-forward → push → 브랜치 정리 | dev 09-08 그대로. 미커밋 54→**125파일** | ✅ 반영. `origin/dev`=`e15f070`, `feat/erp-tax-desk` 커밋·push, 죽은 브랜치 정리 |
| 2 | 🔴 **부분** | 가상 데이터 금지 규칙(AGENTS.md) + `{status, asOf, source}` + Codex 7건 마감 | Codex 7건은 09-18 맥에서 마감 ✅(미커밋). `statements.list`·`fundamentals`에 `{status, items}` ✅. **AGENTS.md에는 세무 조문 규칙(9번)만 추가되고 "통계·기사·과거값 생성 금지"는 없음** | ✅ 반영. AGENTS·CLAUDE·ChatGPT 11번(생성 금지·status·DEMO_MODE). verification ✅/⏸ 규칙 |
| 3 | 🔴 **미반영** | `realty-*.ts` 상수 557개 → JSON + asOf/source/verifiedBy | 그대로. 거기에 세율표·호봉표·요율이 코드 상수로 추가됨(출처 URL은 있음, `asOf`·검증자 없음) | ✅ 구조 반영. `packages/core/data/**` 20파일, entries 992·전원 `미검증`. 원문 대조는 후속 |
| 4 | 🟠 **미결정** | 부동산 터미널 범위 결정 (사용자) | 답 없음. 대신 **ERP(4단계 Accounting/Labor)가 통째로 앞당겨짐**. 로드맵 MVP 1에 ERP 항목 6개가 `[x]`로 추가 | ⏸ 사용자 결정 대기 |
| 5 | 🟠 미반영 | 검증 표 통계 행 ✅는 원문 대조일 때만 | 규칙 미추가. 다만 09-18 Grok 검증은 "숫자 진위 검증 아님"을 스스로 명시 — 좋은 습관 | ⏸ 규칙 문구는 추가됨. 통계 행 원문 대조 ✅는 아직 없음(사용자·후속) |
| 6 | 🟠 미반영 | 진행 로그 하루 1항목, 검증 파일 모델당 하루 1개 | 09-18 진행 항목 17개 | 부분. 습관 과제는 남음 |
| 7 | 🟡 미반영 | ADR 0025 중복, 부동산 ADR 통합 | 중복 그대로. ADR 47개 | ✅ 번호 중복만 해소(백엔드·AI → **0049**). 부동산 ADR 통합은 미착수 |

## 새로 생긴 문제

| # | 심각도 | 항목 | 조치 | 반영 (2026-09-23) |
|---|---|---|---|---|
| A | 🔴 | **ERP·세무 화면이 저장 없이 계산만 한다.** `/erp` 근로자 명부·자격취득·분개·부가세 워크시트·법인세 서식이 전부 "원장 저장 없음, 스키마 후". 입력하면 새로고침에 사라짐. 화면 4,800줄이 스키마 없이 먼저 만들어짐 | 스키마 없는 화면은 더 만들지 않는다. ERP는 (a) 사용자가 실제로 쓸 역할 하나(본인=근로자 투자자면 `/profile 12 세무·회계`만) 남기고 (b) 나머지 `/erp` 역할(개인사업자·법인·공무원 호봉)은 **4단계로 되돌리고 라우트에서 내리기** | ⏸ 사용자 결정 대기 |
| B | 🔴 | **세법·요율 상수가 코드에 박히는 패턴이 부동산에서 세무로 번짐.** `tax-brackets.ts`(양도·증여·종부세), `corp-tax.ts`, `labor-cost.ts`(고용보험 요율), `labor` 호봉표(공무원보수규정 별표 3, 2026.1.2), `year-end-deductions.ts`. 출처 URL·조문은 있으나 시행일·개정 추적 없음. 09-13 `payroll.ts` 요율 지적과 같은 문제가 10배 | `packages/core/data/tax/*.json`으로 분리, 항목마다 `{effectiveFrom, effectiveTo, sourceUrl, article, verifiedBy, verifiedOn}`. 화면에 "기준: 2026-01-02 시행 별표 3" 표기. 연 1회 갱신 체크리스트를 `docs/tech/tax-tables.md`에 | ✅ 반영. `data/tax/*.json` + `docs/tech/data-tables.md`. 전원 미검증 |
| C | 🟠 | 윈도우에 `feat/real-estate-market-analytics` 브랜치가 따로 있고(09-18 기록) 맥 작업 트리와 통합 안 됨. 두 기기가 서로 다른 미커밋·미푸시 상태 | 윈도우 브랜치 push → 맥에서 확인. 기기 간 이동은 반드시 push/pull로만(`docs/tech/git-workflow.md`에 이미 있는 규칙) | ⏸ 미착수 |
| D | 🟠 | `profile/page.tsx` 1,133줄(09-13 1,016 → 09-16 1,106 → 1,133). 메뉴 12개 | 09-13·09-16 동일 제안. 더 미루면 이 파일이 병목 | ⏸ 미착수 |
| E | 🟡 | AGENTS.md 영문 항목 번호가 9→10으로 밀려 한국어(9개)와 어긋남 | 한국어에도 10번 브랜치 규칙 추가하거나 영문 정리 | ✅ 한/영 9~11 맞춤 |

## 잘된 것
- Codex 7건이 09-18 맥에서 전부 닫힘. `{status: ok|empty|unavailable}` 패턴이 `fundamentals`·`statements`에 자리 잡음. 검증 파일이 "DB 꺼진 상태"와 "켠 뒤"를 구분해 기록
- 세무 화면 원칙(ADR 0040 "조문 문언만, 애매하면 근로소득, 유튜브 해석 금지")은 방향이 옳다. 이 문장을 데이터 규칙(생성 금지)으로 확장하면 09-16 #2가 된다
- 타입체크는 125파일 미커밋 상태에서도 통과

## 지금 당장 할 것 — 순서대로, 다른 작업 금지

```bash
# 1. 현재 미커밋분을 하나의 브랜치로 커밋 (10분)
git checkout -b feat/erp-tax-desk          # 미커밋 125파일은 대부분 ERP·세무
git add -A && git commit -m "feat(erp,tax): ERP 업무홈·세무 화면·Codex 7건 반영 (09-16~09-18 미커밋분)"
git push -u origin feat/erp-tax-desk

# 2. dev를 09-16까지 따라잡기 (선형이라 충돌 없음)
git checkout dev && git merge --ff-only e15f070 && git push origin dev

# 3. 죽은 브랜치 정리 (전부 dev에 포함됨)
git branch -d feat/financial-profile feat/quote-targets feat/insurance-coverage \
  feat/news-dashboard feat/opinions-dashboard feat/statements-reader feat/invest-dashboard \
  feat/first-run fix/first-run-review docs/first-run-review feat/accounts-csv
```

그다음 사용자가 결정할 것 두 가지 (AI가 대신 정하지 말 것):
1. **부동산 터미널**: Brief 10번째 모듈로 정식 편입 / 동결
2. **ERP(개인사업자·법인·공무원)**: 지금 본인이 쓸 역할이 아니면 4단계로 되돌리고 라우트에서 내림 / 계속 (그렇다면 스키마부터)

결정 전까지 AI 세션에 줄 지시는 한 줄이면 된다: **"새 화면·새 ADR 금지. 커밋·머지·규칙 추가·상수 JSON화만."**

## 09-13 → 09-16 → 09-21 추이
| | 09-13 | 09-16 | 09-21 |
|---|---|---|---|
| dev 최신 | 09-08 | 09-08 | 09-08 |
| 미커밋 | 31+45 | 54 | 125 |
| ADR | 6 | 31 | 47 |
| 화면(라우트) | 4 | 12 | 13 (+`/erp`) |
| `profile/page.tsx` | 1,016 | 1,106 | 1,133 |
| 🔴 미해결 | — | 3 | 3 + A·B |
