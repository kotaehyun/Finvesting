# 2026-09-25 — 상수 JSON화(3단계) 검토 (Claude, Cowork)

- 범위: 09-23 Cursor 커밋 `99a5156 feat(core): 통계·세율 상수를 packages/core/data JSON으로 분리` (68파일 +18,534/−1,857) + 규칙 보강
- 기준: `feat/erp-tax-desk` = `origin` = `99a5156`. 작업 트리 깨끗. `dev` 09-16 그대로(지시 대기 중 — 정상)
- 실행: 리눅스 VM(Node 22) 타입체크 — core·api·worker ✅, **web ❌ 3건**(아래 #1)

## 결론
형태는 갖췄다 — JSON 20개, 메타 필드, 스키마 테스트, `data-tables.md`, AGENTS 11번 보강, 검증 파일까지. 그런데 **메타가 값과 연결돼 있지 않다.** 코드는 JSON의 원래 객체(`rates`, `exports.X`)를 읽고, `entries[]`는 같은 숫자를 한 번 더 적어 둔 별도 목록이다. 값을 고치려면 두 곳을 고쳐야 하고 둘이 어긋나도 아무 테스트도 잡지 못한다. 거기에 테스트가 "전부 미검증"을 **정답으로 고정**해서, 첫 값을 원문 대조하는 순간 테스트가 깨진다. 09-21 프롬프트가 원한 "AI가 숫자를 바꿔도 잡히는 구조"는 아직 아니다.

| # | 심각도 | 내용 | 조치 | 반영 (2026-09-25 Cursor) |
|---|---|---|---|---|
| 1 | 🔴 | **web 타입체크 실패** `realty/people-panel.tsx` 46·85·105 `implicitly any` — `realty-officials.ts` 등이 `(dataFile as any).exports.X as any`로 export해 타입이 사라짐. Cursor는 "루트 pnpm 깨짐"으로 core만 검사하고 web을 안 돌려 못 봄. 진행 로그엔 "core tsc·vitest 230 통과"만 | `as any` 제거(아래 #3)로 해결됨. 그 전엔 dev 머지 금지. pnpm 복구(`corepack enable` 또는 `npm i -g pnpm@9`) 후 `pnpm typecheck` 8패키지 | ✅ `as any` 0. typecheck 8/8 |
| 2 | 🔴 | **`entries[]`가 값과 분리된 복제본.** 예: `payroll-rates-2026.json`은 `rates.nationalPensionEmployee: 0.0475`(코드가 읽음)와 `entries[1].value: 0.0475`(메타만)가 따로. `realty/*.json`은 `exports.REALTY_X`(코드) + `entries`(메타). 1,012개 entries 전부 이 구조 | 둘 중 하나로: (A) 코드가 `entries`에서 id로 읽게 하고 원래 객체 삭제 — 정석. (B) 원래 객체를 유지하되 `entries`는 없애고 **파일 단위 `defaults` 메타만** 두기 — 현실적. 15개 realty 파일은 `defaults` 하나로 충분하니 B, `tax/*` 5개는 값이 적으니 A. `dump-data-json.mjs`가 `collectNumberEntries`로 복제본을 만들었으니 스크립트도 정리 | ✅ tax=A(`getValue`), realty=B(entries 삭제·exports 승격). dump 스크립트 삭제 |
| 3 | 🔴 | **`as any` 121회**(realty 15파일 + tax-brackets 11 + labor-cost 8 + payroll 1). JSON 분리 전엔 타입이 있던 상수가 전부 `any`가 됐다. #1이 그 첫 증상 | `tsconfig` `resolveJsonModule`은 이미 켜져 있으니(`include: ["src","data"]`) JSON 리터럴 타입을 그대로 쓰거나, `satisfies` + 기존 타입 선언으로. `as any` 0을 완료 조건에 | ✅ core `as any` 0. 타입 단언으로 export 복원 |
| 4 | 🟠 | **테스트가 검증을 막는다.** `data-tables.test.ts` "미검증 항목 수를 집계한다"가 `expect(unverified).toBe(total)` — 값 하나를 원문 대조해 `verifiedBy: "사용자"`로 바꾸면 실패 | `toBe(total)` → 집계를 `console.info`로 출력만 하고, 검증 로직은 "허용값인지"까지만 | ✅ |
| 5 | 🟠 | `VERIFIED_BY = ["사용자","Grok","Codex","미검증"]` — Claude·Gemini·Cursor가 없음. 갱신 절차 2번 "verifiedBy를 사용자 등으로"가 막힘 | `"사용자" \| "AI:<모델>" \| "미검증"` 형태로 열거 대신 패턴 검사 | ✅ |
| 6 | 🟠 | 화면 「기준·출처」: `/realty` 패널은 `asOf` 표시가 있으나 `/erp`·`/profile` 세율·요율·호봉 화면엔 없음 (09-23 로그도 "빠진 곳" 인정) | `tax/*` 5개 파일의 `defaults.asOf`·`sourceTitle`을 해당 화면 하단 한 줄로. 09-21 3단계 5번 항목 | ✅ `dataSourceLine` + erp/profile 하단 |
| 7 | 🟡 | `realty/loans.json`·`ref.json` entries 0 — 숫자 없는 라벨 파일. `map.json` 131 entries는 지도 좌표·스타일 값(통계 아님)에 메타가 붙음 | 라벨·좌표 파일은 `data/`가 아니라 `src/`에 남기거나 `kind: "config"`로 구분해 검증 대상에서 제외 | ✅ `kind:"config"` (map/ref/loans) |
| 8 | 🟡 | 검증 파일 `2026-09-21-cursor.md`는 있으나 web·api 타입체크 "못 함"이 빠짐(pnpm 깨짐을 막힌 것에만 적음) | 검증 못 한 것 칸에 명시 — 규칙 | ✅ 이번 검증 파일에 8/8·web build 기록 |

## 잘된 것
- 20파일 전부 `defaults`에 `sourceUrl`·`asOf`·`verifiedBy` 있음(누락 0). 출처 URL이 법원·국세청·4대보험연계센터 등 공식
- 992→1,012개 전부 정직하게 `"미검증"`. 새 숫자 안 만듦
- `data-tables.md`의 갱신 주기·절차 표는 그대로 쓸 만하다
- `{status}`·`DEMO_MODE` 문장 보강, `2026-09-21-claude.md` 반영 열 갱신, 규칙 위반 없음(새 화면·ADR 0)

## 다음 프롬프트 (그대로 사용)
```
docs/prompts/00-bootstrap.md로 시작. docs/review/2026-09-25-claude.md를 읽는다.
이번 세션: 새 화면·라우트·ADR 금지. 같은 폴더에서 다른 AI 세션 동시 실행 금지. 새 숫자 입력 금지.

0. pnpm 복구: `corepack enable && corepack prepare pnpm@9.15.0 --activate` (안 되면 `npm i -g pnpm@9.15.0`). `pnpm typecheck`로 web 실패 3건(realty/people-panel.tsx) 재현을 먼저 보고.

1. packages/core/data 구조 정리 — 변경 파일 목록 먼저 제시, 내 확인 후 실행:
   - tax/*.json 5개: 코드가 `entries`에서 id로 읽는다(`getValue(file, "rates.nationalPensionEmployee")`). `rates`·`brackets` 같은 원래 객체는 삭제. 값은 한 곳에만.
   - realty/*.json 15개: `entries` 삭제, `defaults` 메타만 유지. `exports.X`는 최상위로 올린다. 좌표·스타일·라벨만 있는 map/ref/loans는 `"kind": "config"`를 두고 통계 검증에서 제외.
   - `as any` 0. JSON 리터럴 타입 + `satisfies`로 기존 export 타입 복원. `dump-data-json.mjs`는 삭제하거나 새 구조에 맞춤.
2. data-tables.test.ts: `expect(unverified).toBe(total)` 삭제, 집계는 출력만. `VERIFIED_BY`를 `"사용자" | "AI:<이름>" | "미검증"` 패턴 검사로.
3. `/erp`·`/profile` 세율·요율·호봉 화면 하단에 「기준 <asOf> · <sourceTitle>」 한 줄 (tax/* 5개 defaults에서).
4. `pnpm typecheck` 8/8, `pnpm --filter @finvesting/core test`, `pnpm --filter @finvesting/web build`.
5. docs/prompts/90-session-end.md. 검증 파일에 실행한 것·못 한 것. data-tables.md 규칙 갱신. 리뷰 2026-09-25-claude.md 반영 열.
push는 feat/erp-tax-desk만. dev merge는 내가 지시할 때.
```

## 사용자 결정 대기 (09-16부터)
1. 부동산 터미널 — 편입 / 동결 → **✅ 2026-09-26 동결 (ADR 0050)**
2. ERP(개인사업자·법인·공무원) — 계속(스키마부터) / 보류 → **✅ 2026-09-26 보류 (ADR 0050)**
