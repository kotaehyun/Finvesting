# 2026-09-16 — 2차 종합 검토 (Claude, Cowork)

- 범위: 09-13 검토 이후 3일간의 변화 — 브랜치·커밋 상태, 신규 기능(부동산·펀더멘털·뉴스·보험 등), 데이터 신뢰성, 문서 프로세스
- 기준: `feat/yahoo-fundamentals` HEAD `e15f070`(09-16, 윈도우 push) + 미커밋 54파일. `dev`는 아직 `de705f8`(09-08)
- 실행: 리눅스 VM(Node 22)에서 core·api·worker·web 타입체크 통과(미커밋 포함). 테스트·브라우저·DB는 Codex/Grok 09-16 기록을 인용. 정적 검토 위주
- 09-13 검토 반영 확인: 🔴1~3·🟠4·🟡(트랜잭션, 세액 쿼리, `--hostname 127.0.0.1`) 모두 반영됨. 잘 됐다.

## 결론

09-13에 지적한 "코드는 좋은데 안 커밋되고 방향이 샌다"가 3일 사이 **10배로 커졌다**. 307파일 +42,119줄, ADR 25개(0007~0031), 화면 8개 추가. 그중 부동산 터미널(`/realty`, core `realty-*` 21모듈, ADR 0014~0027)은 기획서 9개 모듈 어디에도 없고, 09-16에 **AI가 만든 가상 통계**(유찰률 68.4%, 미수금 8,940억, 생성 헤드라인)가 실데이터로 화면에 나갔다가 제거됐다. 지금 필요한 것은 기능 추가가 아니라 **범위 동결 + 머지 + 데이터 규칙**이다.

| # | 심각도 | 항목 | 조치 |
|---|---|---|---|
| 1 | 🔴 | **dev가 09-08에 멈춤.** 09-13 이후 커밋 20개가 `feat/financial-profile → quote-targets → insurance-coverage → news/opinions/statements → invest-dashboard → yahoo-fundamentals` 순으로 **한 줄로 쌓여** 있고 어느 것도 dev에 안 들어감. 브랜치 이름 9개가 전부 같은 선 위의 점. 거기에 미커밋 54파일 | 지금 `dev`를 `e15f070`으로 fast-forward(전부 선형이라 충돌 없음) → push → 오래된 feat 브랜치 삭제. 이후 **브랜치 하나 = 기능 하나 = 머지 후 삭제** 규칙을 실제로 지킴. 미커밋분은 `fix/fallback-removal`로 따로 커밋 |
| 2 | 🔴 | **가상 통계가 실데이터 경로에 들어갔다.** Codex 검토 P1 3건: DB 실패 시 지역 대출 17개 고정값+과거값 ×0.96 생성, 펀더멘털 5종 상수 폴백(source=yahoo로 위장), 가짜 기사 6건. 09-16 미커밋 수정으로 제거했다고 하나 아직 커밋·검증 전 | (a) `AGENTS.md`에 규칙 추가: "수치는 수집 성공분만 표시. 수집 실패·키 없음은 빈 칸+상태. **어떤 경우에도 통계·기사·과거값을 생성하지 않는다.** 샘플 데이터는 `DEMO_MODE=true`일 때만, 화면에 '샘플' 배지" (b) 모든 통계 응답에 `{status: ok\|empty\|stale\|unavailable, asOf, source}` 통일 (c) Codex 7건을 커밋 단위로 닫고 검증 파일에 재현 결과 기록 |
| 3 | 🔴 | **핵심 통계가 코드에 상수로 박혀 있다.** `core/realty-*.ts`에 숫자 리터럴 557개(파산 149,146건, 파탄원인 46.65% 등)가 TS 소스에 직접. 출처 URL은 있으나 **수집일·검증자·원문 대조 테스트 없음**. 테스트는 "양수인지"만 확인(Codex 지적). 이 구조에서는 AI가 숫자를 바꿔도 아무도 모른다 | 상수를 `packages/core/data/*.json`으로 빼고 각 값에 `{value, unit, asOf, sourceUrl, sourceTitle, verifiedBy, verifiedOn}` 필수. 값 없는 항목은 `null`. 코드는 이 파일을 읽기만. 검증 표에서 통계 행의 ✅는 "화면 렌더"가 아니라 "원문 대조"일 때만 |
| 4 | 🟠 | **범위 결정 필요: 부동산 터미널.** 3D 지도·시군구 도면·공직자 자산·인구·국세 고가주택·법인 명의·파산 원인·전월세·카드연체까지. 기획서 밖이고 근로자 투자자 페르소나와 연결이 약함. 로드맵은 이걸 포함해 MVP 1 항목을 전부 `[x]`로 바꿈. Financial Timeline만 `[ ]` | 사용자가 결정: (A) 의도한 모듈이면 Brief에 "Realty" 10번째 모듈로 추가하고 **별도 단계**로 빼기 (B) AI 드리프트면 `/realty`를 동결(라우트는 두되 로드맵·다음 할 일에서 제외). 어느 쪽이든 로드맵의 MVP 1은 09-13 정의(매달 쓰는 루프 + Timeline)로 되돌리기 |
| 5 | 🟠 | 검증 표가 가상 통계를 ✅로 통과시켰다 (`/realty` Gemini 유찰 68.4 → 다음날 폐기). 렌더 200 = 검증으로 취급된 결과 | `verification/README.md` 규칙에 추가: 통계·외부 수치 항목은 **원문 URL과 값 대조**를 적어야 ✅. "페이지 200"만이면 ⏸ |
| 6 | 🟠 | 하루 24개 진행 항목(09-15), 검증 파일 18개(grok-1~18)+codex+gemini. 같은 브랜치에 맥(Grok·Codex)과 윈도우(Gemini)가 동시에 커밋 → Codex 검토 중 미커밋 수정 유입 | 기기당 브랜치 분리(윈도우는 `feat/*-win` 또는 dev에서 새로 분기). 진행 로그는 하루 1항목에 소제목으로 묶기. 검증 파일은 모델당 하루 1개에 누적 |
| 7 | 🟡 | ADR 번호 중복 `0025`(대출 금리 / 백엔드·AI 방향). ADR 33개 중 부동산 14개 — 결정이 아니라 작업 기록에 가까움 | `0025-backend-ai-evolution` → `0032`. 부동산 ADR은 하나(`0014-realty-scope`)로 합치고 나머지는 `docs/tech/realty.md` 설계 문서로 |
| 8 | 🟡 | `packages/api/src/routers/market.ts` 722줄에 시장·뉴스·부동산·펀더멘털이 한 파일. `profile/page.tsx` 1,106줄(09-13 1,016 → 증가) | `realty.ts`·`news.ts` 라우터 분리. profile 메뉴별 컴포넌트 분리는 09-13 제안 그대로 |
| 9 | 🟡 | `market_news.embedding`(pgvector) 여전히 미사용, 뉴스 1,246건 수집 중. Expo 모바일 ❌ 09-02 이후 그대로 | 2단계 RAG 전까지는 그대로 두되 로드맵 "2단계"에 명시. 모바일은 "보류"로 표기해 ❌가 계속 뜨지 않게 |

## 잘된 것
- 09-13 검토 1~4 반영: `core/quote-targets.ts`(env는 합집합, KRX `.KS`→`.KQ` 재시도), Yahoo `KRW=X`로 USDKRW, `core/invested.ts`(평가액+예수금) — 설계·테스트·ADR 0007까지 깔끔. 보유 BTC 시세·환율 실행 확인됨
- 🟡 항목도 반영: `workspaceSave` 트랜잭션, 세액 쿼리 `income_tax` 필터, `next dev --hostname 127.0.0.1`
- Codex의 09-16 커밋 검토는 이 저장소에서 가장 좋은 리뷰다(커밋 고정 + DB mock 재현). 이 방식을 `docs/prompts/20-code-review.md`에 표준으로 넣을 것
- 09-16 맥 작업이 가상 통계를 스스로 찾아 제거하고 로그·ADR 0027로 남긴 것

## 09-13 검토와 비교
| 09-13 지적 | 09-16 상태 |
|---|---|
| 미커밋 11세션 | 커밋됨 ✅ — 그러나 dev 미머지, 새 미커밋 54파일 |
| 시세 대상 env 고정 | 해결 ✅ |
| USDKRW 없음 | 해결 ✅ (Yahoo) |
| 투자자산 max() | 해결 ✅ |
| /profile 브라우저 미클릭 | 일부 클릭 확인(09-15 Grok Chrome) |
| 문서 3개 불일치 | status.md·Claude 문서 갱신됨 |
| 범위: Timeline·재무제표 밀림 | 재무제표 `/statements` 생김 ✅. Timeline 여전히 `[ ]`. **대신 부동산 터미널이 생김** |

## 다음 세션 순서 제안
1. dev fast-forward + push + 브랜치 정리 (#1) — 10분
2. 미커밋 폴백 제거분 커밋, Codex 7건 상태표 (#2)
3. AGENTS.md 데이터 규칙 + verification 규칙 한 줄씩 (#2, #5)
4. 부동산 범위 결정 (#4) — 사용자
5. 통계 상수 → JSON with asOf/source (#3) — 결정 후
6. 그다음에야 Financial Timeline
