# 2026-09-15 — 금융 원장 핵심 경로 재검토
- 리뷰어: Codex
- 범위: feat/yahoo-fundamentals 현재 작업 트리. 기존 world-indices.ts 미커밋 변경 포함.
- 환경: Windows, Node v24.17.0, pnpm 9.15.0.
- 제품 코드와 실데이터 수정 없음.

## 발견한 문제
| # | 심각도 | 위치 | 내용 | 반영 |
|---|---|---|---|---|
| 1 | 높음/P1 | packages/core/src/portfolio.ts:38 | 0.3 매수 → 0.1 매도 → 0.2 매도에서 잔량이 0.19999999999999998이 되어 정상 전량 매도를 거부한다. 코인·소수점 주식에 영향. DB 수량 정밀도에 맞춘 정수 단위 연산 또는 허용 오차와 잔량 정규화 필요. | 미반영 |
| 2 | 높음/P1 | packages/api/src/routers/trades.ts:109–113; packages/api/src/lib/holdings.ts:59 | 동일 시각의 체결 검증은 신규 ID new를 써서 마지막에 정렬하지만 저장은 임의 UUID다. 매수 ffff…, 매도 1111…이면 생성 검증은 통과해도 이후 UUID 순 재검증은 매도가 먼저여서 실패한다. 보유 조회는 시각만 정렬해 순서도 불확정. 영속적인 체결 순번을 도입해 생성·삭제·조회 순서를 통일해야 한다. | 미반영 |
| 3 | 높음/P1 | packages/core/src/account-balance.ts:24–35 | 하루치 내림차순 파일은 첫 날짜=마지막 날짜라 오름차순으로 간주한다. 최신 잔액 800, 이전 잔액 900 순 입력에서 900을 선택해 계좌 잔액을 잘못 덮어쓴다. 거래 시각/정렬 방향을 보존하고 불명확하면 사용자 선택이나 잔액 반영 보류 필요. | 미반영 |
| 4 | 중간/P2 | packages/api/src/routers/transactions.ts:147–151 | 날짜·금액·방향·메모·거래처가 같은 실제 반복 거래를 Set으로 제거한다. 같은 날 같은 가맹점에서 동일 금액을 두 번 결제하면 1건만 저장. 은행 거래 ID 또는 동일 거래 출현 횟수와 배치 식별 필요. 9/14 리뷰 #7 미해결. | 미반영 |
| 5 | 중간/P2 | packages/api/src/lib/overview.ts:64 | accounts API가 외화 계좌를 허용하지만 currency 없이 잔액과 현금흐름을 합산한다. USD 1000이 원화 합계에서 1000으로 계산된다. 통화 제한 또는 환산 정책 필요. 9/14 리뷰 #8 미해결. | 미반영 |

## 검증
- pnpm typecheck: 8/8 패키지 통과.
- pnpm --filter @finvesting/core --filter @finvesting/interop test: core 139, interop 11 통과.
- Node로 실제 core 소스를 import하여 #1·#3 재현. #2는 라우터 정렬식을 적용한 core 호출로 생성 검증 성공과 영속 UUID 순서 실패를 재현.
- #4·#5는 정적 검토. DB 통합, 동시성, 브라우저, 외부 수집, Ollama, 프로덕션 빌드는 이번에 실행하지 않음.
- 기존 테스트 통과만으로 위 경계 사례의 정확성이 보장되지 않는다.

## 개선 제안
1. #1–#3 수정과 경계 사례 회귀 테스트.
2. #4–#5 데이터 보존·통화 정책 확정 및 구현.
3. 테스트 DB에서 가져오기 → 분류 → 잔액·손익 대조를 통합 검증.

## 잘된 점
- 이전 리뷰의 내부 이체 분류, 저장 트랜잭션, 과매도 검사, 기본 localhost 바인딩이 코드에 반영됨.
- core 계산과 API 저장 역할이 분리되어 재현이 용이함.


## 수정 작업 프롬프트
[금융 원장 오류 5건 수정](../prompts/21-ledger-correctness-fixes.md): 새 작업에 붙여 넣을 실행 지시와 완료 조건. 프롬프트 작성만 완료했으며 오류 반영 상태는 미반영 유지.

