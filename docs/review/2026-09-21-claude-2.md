# 2026-09-21 — 프롬프트 반영 검토 (Claude, Cowork, 같은 날 2차)

- 범위: 09-21 3차 검토 프롬프트(커밋·머지 / 데이터 규칙 / 상수 JSON화 / 세션 종료) 실행 결과
- 기준: `feat/erp-tax-desk` HEAD `21ed1ae`. 작업 트리 깨끗. VM(Node 22)에서 core·api·web 타입체크 통과

## 결론
**1·2단계는 됐다.** 3주 만에 dev가 움직였고(09-08 → 09-16), 미커밋 125파일이 5커밋으로 정리돼 원격에 올라갔다. 3단계(상수 JSON화)는 시작 안 했고, 4단계(세션 종료 절차)는 절반만 됐다. 그리고 프롬프트 최상단 규칙 "새 화면·새 ADR 금지"를 어긴 작업이 하나 섞여 들어갔다.

| 단계 | 상태 | 확인 |
|---|---|---|
| 1 커밋·머지·브랜치 | ✅ | `dev`=`origin/dev`=`e15f070`. `feat/erp-tax-desk` 5커밋(core→api/web→web→docs→docs), 4개 push. 죽은 브랜치 11개 삭제(로컬 3개만 남음). 커밋 단위·메시지 형식 좋음 |
| 2 데이터 규칙 | ✅ 축약 | AGENTS·CLAUDE·ChatGPT 11번 "공식 출처만, 생성 금지, 없으면 비움" 한/영. verification 규칙 1줄. ADR 0025 → **0049**(0048은 이미 사용됨, 아래) |
| 3 상수 → JSON | ❌ 미착수 | `packages/core/data/` 없음. `docs/tech/data-tables.md` 없음. 보고·멈춤 규칙대로 대기 중일 수 있음 |
| 4 세션 종료 | ⚠ 절반 | 진행 로그·ADR·상태 문서는 갱신. **`docs/verification/2026-09-21-*.md` 없음**(타입체크·테스트 실행 기록 없음). 리뷰 문서 "반영" 열 미갱신 |

## 문제

| # | 심각도 | 내용 | 조치 |
|---|---|---|---|
| 1 | 🟠 | **규칙 위반 1건이 커밋에 섞임.** 진행 로그 최상단 "투자 봇AI 대화형 화면"(`/chat` 개편, ADR 0048 chat-composer)은 이 프롬프트가 금지한 새 화면·새 ADR. 별도 세션 결과물이 `044b3e7 docs:` 커밋에 함께 들어갔다("docs" 커밋에 `/chat` UI 변경). 프롬프트가 "각 단계마다 멈춤"을 요구했으나 그 사이 다른 AI 세션이 돌아간 것으로 보임 | 되돌릴 필요는 없다(작동하고 타입체크 통과). 단 **동시에 두 AI 세션을 같은 작업 트리에서 돌리지 않을 것** — 09-16 Codex 검토 때도 같은 일이 있었다. 한 기기 = 한 세션 |
| 2 | 🟡 | 마지막 커밋 `21ed1ae`(규칙 추가) 미push | `git push` |
| 3 | 🟡 | AGENTS 11번이 프롬프트 문안보다 짧음: `{status: empty\|stale\|unavailable}` 표기 의무와 `DEMO_MODE` 샘플 규칙이 빠짐. 지금 코드는 `{status, items}`를 쓰고 있어 규칙과 코드가 어긋나진 않지만, 다음 AI가 status 없이 빈 배열만 돌려줘도 규칙 위반이 아니게 됨 | 11번에 한 문장 추가: "실패·부재는 `{status: empty\|stale\|unavailable}`로 응답한다. 샘플 데이터는 `DEMO_MODE=true`에서만, 화면에 「샘플」 배지" |
| 4 | 🟡 | 검증 파일 없음. 1·2단계는 git 작업이라 검증할 코드가 없지만 "타입체크 통과 후 커밋"을 했다면 그 기록이 있어야 함 | 3단계 끝낼 때 `2026-09-21-<모델>.md` 하나에 묶어 기록 |
| 5 | 🟢 | `docs/progress/status.md`가 Claude 문서 복사본 형식이 아니라 커밋 안에서 한 줄만 바뀜(`2 +-`) | 3단계 후 갱신 |

## 잘된 것
- fast-forward만 쓰고 `-D` 안 씀. `--ff-only` 지시 준수. 브랜치 이름 정리 후 `git branch -a`가 읽힌다
- 규칙 문안을 세 파일에 같은 번호로 넣고 영문까지 맞춤. 한/영 번호 어긋남(09-21 E) 해소
- ADR 번호 충돌을 스스로 감지해 0048 대신 0049로 — 판단 맞음

## 다음 프롬프트 (그대로 사용)
```
docs/prompts/00-bootstrap.md로 시작. docs/review/2026-09-21-claude-2.md를 읽는다.
이번 세션: 새 화면·라우트·ADR 금지. 다른 AI 세션을 같은 폴더에서 동시에 돌리지 않는다.

0. git push origin feat/erp-tax-desk (21ed1ae 미push)
1. AGENTS.md·CLAUDE.md·ChatGPT.md 11번에 한 문장 추가(한/영):
   "실패·부재는 `{status: empty|stale|unavailable}`로 응답한다. 샘플 데이터는 `DEMO_MODE=true`에서만, 화면에 「샘플」 배지."
2. 09-21 프롬프트 **3단계**(상수 → packages/core/data/**.json)를 그대로 수행. 변경 파일 목록 먼저 제시 → 내 확인 → 실행.
   verifiedBy가 "미검증"인 항목 수를 보고에 명시. 새 숫자 채우지 않음.
3. docs/prompts/90-session-end.md. docs/verification/2026-09-21-<모델>.md에 타입체크·테스트 실행 결과.
   docs/review/2026-09-21-claude.md 표 "반영" 열 갱신(1·2·B·7 반영, 3은 이번 결과, 4·5·A 사용자 결정 대기).
   docs/progress/status.md를 Claude 프로젝트 문서 형식으로 갱신.
push는 feat/erp-tax-desk만. dev merge는 내가 지시할 때.
```

## 사용자 결정 대기 (변경 없음)
1. 부동산 터미널 — 편입 / 동결
2. ERP(개인사업자·법인·공무원) — 계속(스키마부터) / 보류
