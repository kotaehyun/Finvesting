# 2026-09-05 — Cursor Grok (계좌·CSV)
- 환경: 맥북, `feat/accounts-csv`, `pnpm dev:web` 이미 기동 중 (Next 15.5.25), Docker Postgres
- 범위: `/accounts` + interop CSV 가져오기

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| api typecheck | `pnpm --filter @finvesting/api typecheck` | 통과 (`merchant` nullish 수정 후 commit 포함) |
| `GET /accounts` | curl | 200 |
| 계좌 생성 | POST `accounts.create` | 200, id 발급, type=checking |
| CSV 미리보기 | POST `transactions.previewImport` 범용 통장 3행 | detected=`generic-bank`, rows=3 |
| CSV 저장 | POST `commitImport` | inserted=3, balanceUpdated=3150000 |
| 중복 재업로드 | 같은 rows 다시 commit | inserted=0, duplicate=3 |
| 대시보드 반영 | GET `dashboard.overview` | accountCount=1, liquid/net=3150000, income=3200000, variableCost=50000 |

## 추가 검증됨 — 2026-09-06 Chrome UI
시스템 Chrome + puppeteer-core로 `/` → `/accounts` 폼 입력 → 파일 선택 → 미리보기 → 저장 → 대시보드 재조회.

| 항목 | 결과 |
|---|---|
| 대시보드 기존 데이터 | 순자산 3,150,000 · 수입 3,200,000 · 변동 50,000 |
| 계좌 추가 폼 | `UI검증 입출금` / 토스뱅크 / 입출금 생성, 목록에 나타남 |
| CSV 업로드 미리보기 | `generic-bank` 2건: 용돈 입금 500,000, 커피 출금 15,000 |
| 저장 | 화면 문구 `저장 2건 · 잔액 485,000원`, 최근 거래에 용돈·커피 |
| 대시보드 갱신 | 순자산 3,635,000 · 수입 3,700,000 · 변동 65,000 |

## 검증 못 함
- XLSX·EUC-KR 실파일 (UTF-8 CSV만)
- 은행별 전용 파서
- 한글 IME 입력 (자동화는 `type`)

## 교차 검증
- 없음
