# 2026-09-14 — Codex

- 환경: macOS, Node v26.7.0, 프로젝트 packageManager pnpm@9.15.0. Node 22 기준 환경에서는 재실행하지 않음.
- 범위: `feat/yahoo-fundamentals` 현재 작업 트리의 문서 기반 종합 검토.

## 검증됨 (실제 실행)

| 항목 | 방법 | 결과 |
|---|---|---|
| 현재 상태 | `git status --short`, `git branch --show-current` | feat/yahoo-fundamentals, 기존 수정·미추적 파일 존재 |
| 전체 타입 검사 | `pnpm typecheck` | 실패(exit 2). 7/8 패키지 성공, web 오류 9개. ReactMarkdown 4, Suspense 1, forwardRef 패널 3, ReactNode 전달 1 |
| core 테스트 | `pnpm --filter @finvesting/core --filter @finvesting/interop test` | core 20파일 89개 통과 |
| interop 테스트 | 위 명령 | interop 2파일 9개 통과 |
| 과거 매도 계산 | Node에서 실제 `buildPositions` import, 매도 1 → 매수 1 | 수량 1, 실현손익 0. 잘못된 매도가 오류 없이 무시됨 |
| 내부 이체 집계 | Node에서 실제 `summarizeCashflow` import, 입출금 각 10만 원 | 수입 10만·변동비 10만·소비율 100% |
| 잔액 선택식 | 내림차순 날짜의 두 행에 현재 reverse/find 적용 | 오래된 날짜의 잔액 선택. DB 연동 시험은 아님 |

## 정적 검토

- AGENTS, 부트스트랩/종료/리뷰 규칙, README, 진행 로그 최신 구간·로드맵·상태, 기술 개요·아키텍처·데이터 모델·AI·파일 연동·수집·설정, ADR 0007~0010, 이전 종합 리뷰·검증 현황 등을 읽음.
- 주요 API의 계좌/거래/체결/프로필 저장, overview/holdings, portfolio/cashflow, Yahoo 수집, worker 스케줄, 챗 컨텍스트 및 Ollama 프로바이더 확인.
- 상세 발견 사항은 `../review/2026-09-14-codex.md`.

## 검증 못 함 / 다음 작업

- 프로덕션 build, 실제 브라우저 저장·클릭, DB 롤백/중복 업로드 동시성/실원장 대조.
- 외부 수집기·Ollama·Expo·윈도우·Node 22 실행, 최신 세율·보험요율.
- 타입 오류의 정확한 의존성 유입 원인. React 18/19 타입 공존과 진단은 확인했지만 수정으로 원인을 확정하지 않음.
- 제품 코드·DB·환경설정 변경, 커밋·푸시·머지 없음. pnpm 임시 파일·검사 캐시는 생성될 수 있음.

## 교차 검증

- 2026-09-14 Grok의 core 89·interop 기존 9 테스트 통과를 재확인.
- 최근 기록의 web React JSX 오류를 재현. 전체 타입 검사는 통과 상태가 아님.
- 2026-09-13 Claude 리뷰의 저장 원자성·체결 검증·LAN 바인딩 문제는 여전히 존재. 과거 매도 문제는 음수 잔고보다 매도가 축소/무시되는 동작으로 구체화.
