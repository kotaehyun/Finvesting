# 2026-09-13 — Claude (Cowork, Fable 5.1)
- 환경: 맥북 폴더를 Cowork 리눅스 VM(arm64, Node 22.23.2, 맥의 `node_modules` 그대로 사용)에 마운트. pnpm·vitest 네이티브 바이너리(darwin)는 VM에서 못 돌려 core 테스트는 클라우드 컨테이너(Node 22.22.2)에서 vitest 호환 셈(shim)으로 실행. Docker·브라우저·Ollama는 실행 안 함.
- 범위: `feat/financial-profile` 작업 트리(미커밋 포함) 정적 검토 + 타입체크 + core 테스트 교차 검증. 결과는 `docs/review/2026-09-13-claude.md`.

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| 타입체크 core·db·interop·ai·api·worker·web (7패키지) | VM에서 `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json` 패키지별 | 전부 오류 0. **web 포함** — 09-05 "react-markdown React 19 타입 불일치" 블로커 해소 확인 |
| core 테스트 | 클라우드에서 `node --experimental-strip-types --test src/*.test.ts` (vitest → node:test 셈) | 37/37 통과, 22 suites — Grok 09-12 결과와 일치 |

## 정적 검토 (실행 없이 코드 읽기)
| 항목 | 판단 |
|---|---|
| `packages/api` 라우터 8개, `lib` 6개, `core` 11모듈, `db` 스키마 8개, `profile/page.tsx` | 리뷰 문서 #1~#14 참조. 타입·테스트 통과와 별개로 시세 대상 고정(#2)·USDKRW 부재(#3)·투자자산 합산(#4)은 설계 수정 필요 |

## 검증 못 함 / 다음 AI에게
- interop 테스트(xlsx·papaparse 필요) — 맥에서 `pnpm --filter @finvesting/interop test`
- mobile 타입체크(expo 의존) — 맥에서
- `/profile` 브라우저 클릭 전 메뉴 — 사용자 수동
- `0001~0004` 마이그레이션이 다른 기기에서 깨끗이 적용되는지 — 커밋 후 윈도우 clone 때

## 교차 검증
- Grok 09-12 "core 37개 통과" 재확인(다른 실행기, 다른 Node). Cursor 09-02 "타입체크 8패키지"는 7패키지(mobile 제외) 재확인.
