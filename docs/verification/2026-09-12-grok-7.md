# 2026-09-12 — Cursor Grok (프로필대장 투자내역)
- 환경: 맥북, `pnpm dev:web` localhost:3000, Docker Postgres
- 범위: `/profile` 09 투자내역 — 여러 계좌 × 주식/ETF/채권/펀드/코인

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| core 테스트 | vitest | 37개 통과 (holdings-summary 4) |
| api·web typecheck | tsc | 통과 |
| `/profile?menu=invest` | GET | 200. HTML에 erp·투자내역 |
| `/holdings` | GET | 200 |
| `trades.holdings` | tRPC | 열린 포지션 1(BTC 0.001, 검증 코인). 평가 108,852 · 원가 97,966.8. byClass 주식·ETF·채권·펀드 0 · 코인 1. byAccount `검증 코인` |
| `trades.list` | tRPC limit 10 | 계좌명·기관·자산군 필드 있음. BTC · crypto · 검증 코인 |

## 정적 검토
| 항목 | 판단 |
|---|---|
| 계좌 카드 + 자산군 탭 + 보유·체결 그리드 | `invest-panel.tsx`. 클릭 필터는 미실행 |
| 포지션을 계좌×종목으로 계산 | `loadHoldings`가 계좌별 `buildPositions` |
| 매도 수량을 그 계좌 보유만 검사 | `trades.create`. 업비트 BTC가 있어도 빈 증권 계좌 매도는 거절해야 함. 실행 확인 안 함 |

## 검증 못 함 / 다음 AI에게
- Chrome에서 계좌 카드·자산군 탭 클릭
- 증권 2계좌에 같은 종목 체결을 넣고 행이 둘인지 확인 (로컬 데이터 변경이라 이번 세션에서 삽입하지 않음)

## 교차 검증
- 없음
