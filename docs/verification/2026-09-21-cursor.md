# 2026-09-21 — Cursor (Composer)

- 환경: macOS darwin 25.6, Node v26.7.0, packages/core 로컬 `npx tsc` / `npx vitest` (루트 pnpm CLI 링크 깨짐으로 core만 직접 실행)
- 범위: AGENTS 11번 status·DEMO_MODE 문장, 상수 → `packages/core/data/**.json`, 세션 종료 문서

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| core 타입체크 | `cd packages/core && npx tsc --noEmit` | 통과 (오류 0) |
| core 테스트 | `cd packages/core && npx vitest run` | 62파일 **230** 테스트 전건 통과 |
| data JSON 스키마 | `vitest run src/data-tables.test.ts` | 3테스트 통과. entries **992**개, `verifiedBy: "미검증"` **992**개 |

## 정적 검토 (실행 없이 코드 읽기)
| 항목 | 판단 |
|---|---|
| JSON import·export 이름 유지 | realty·tax·payroll·labor·civil-pay 로더가 기존 export를 유지 |
| 새 숫자 미추가 | 덤프 스크립트가 기존 TS export만 옮김 |

## 검증 못 함 / 다음 AI에게
- 루트 `pnpm typecheck`(web·api 포함) — pnpm CLI 경로 깨짐. 재설치 후 재실행
- 브라우저·원문 대조 — 통계 행 ✅로 올리지 않음(전부 미검증)
- 화면 「기준 YYYY-MM-DD · 출처」 전수 확인 — 기존 `asOf`/`source` 필드 있는 객체만 표시되는 곳은 유지. 빠진 UI는 후속

## 교차 검증
- 없음
