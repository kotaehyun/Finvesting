# 2026-09-12 — Cursor Grok (파일 변환·추이)
- 환경: 맥북, 기존 `pnpm dev:web` localhost:3000, Docker Postgres
- 범위: CSV/엑셀/워드 변환, 통장내역, 세액·소비·저축·투자 그래프

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| core 테스트 | `pnpm --filter @finvesting/core test` | 19개 통과 (trends 3 포함) |
| interop 테스트 | `pnpm --filter @finvesting/interop test` | 9개 통과 (xlsx·docx 왕복 포함) |
| api·web typecheck | tsc | 통과 |
| `/profile?menu=trends` | GET | 200. HTML에 추이·통장내역 |
| `profile.templates` | tRPC | recurring/bank 각 csv·xlsx·docx |
| `profile.previewFile` | 통장 CSV | detected=bank, 거래 2건 |
| `profile.previewFile` | 고정비 CSV/xlsx/docx | detected=recurring. docx 표 행 일치 |
| `profile.trends` | month=2026-09, count=12 | 12포인트. 7·8·9월 세액 24만/24.5만/25만, 9월 소비 65,000 |
| `transactions.listAll` | month=2026-09 | 6건, accountName 포함 |

## 정적 검토
| 항목 | 판단 |
|---|---|
| 막대+선 SVG, X/Y 축·표 | `combo-chart.tsx`에 있음. 클릭·호버 미실행 |
| `.doc` 거부 | 코드에서 안내 문구 |

## 검증 못 함 / 다음 AI에게
- 화면에서 가져오기 → 변환 내용 → 통장으로 가져오기 클릭
- 차트 4개가 한 화면에 동시에 그려지는지 Chrome 확인

## 교차 검증
- 없음
