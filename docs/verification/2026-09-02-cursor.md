# 2026-09-02 — Cursor
- 환경: 맥북 본체, Node 22, pnpm 9 (`pnpm install` 실행 가능)
- 범위: 스캐폴딩 전 폴더 정적 리뷰 + 설치·타입체크·테스트
- 출처: `docs/review/2026-09-02-scaffold.md`, `docs/progress/README.md` (3)(4), `docs/logs/2026-09-02-*.md`

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| `pnpm install` | 맥 터미널 | 완료, `pnpm-lock.yaml` 생성 |
| 타입체크 | `pnpm typecheck` | 8패키지 통과 (수정 후) |
| core 테스트 | `vitest` | 7개 통과 (allocation·cashflow·portfolio) |
| interop 테스트 | `vitest` | 3개 통과 (parse-file) |
| mobile 타입체크 | `pnpm --filter @finvesting/mobile typecheck` | 통과 (tsconfig 인라인 후) |
| IDE `expo/tsconfig.base` 오류 | Cursor Problems 패널 | 해결 (logs 기록) |

## 정적 검토 (실행 없이 코드 읽기)
| 항목 | 판단 |
|---|---|
| 대시보드 월 말일 31 고정 | 버그 확정 (Postgres date 범위) — 수정 |
| api `drizzle-orm` 의존 누락 | 버그 확정 — 수정 |
| 루트 `.env` 미로딩 (worker/drizzle/Next) | 버그 확정 — 수정 |
| `instruments` 유니크 부재, watchlist FK 부재 | 수정 |
| yahoo 시세일 UTC | 수정 (KST) |
| 패키지 의존 방향 | 대체로 준수 |

## 검증 못 함 / 다음 AI에게
- DB 마이그레이션·seed·web 기동·worker 실행·Ollama·Expo 기동 전부 미실행
- `yahoo-finance2@2.14.2` deprecated 경고 — 시그니처 런타임 확인 필요
- 리뷰 #18(모바일 env) #19(fxRate) #21(lint) #22(기동 시 수집) 보류 → Claude가 처리

## 교차 검증
- Claude 스캐폴딩 코드 전체를 정적 리뷰 (23개 항목)
