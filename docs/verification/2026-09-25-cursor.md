# 2026-09-25 — Cursor (Composer)

- 환경: macOS darwin 25.6, Node v26.7.0, pnpm 9.15.0 (`COREPACK_ENABLE_STRICT=0` + `/opt/homebrew/bin/pnpm`)
- 범위: 09-25 리뷰 반영 — tax/realty JSON 구조, `as any` 0, verifiedBy 패턴, erp/profile 기준·출처

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| typecheck 8패키지 | `pnpm typecheck` | 전부 통과 |
| core 테스트 | `pnpm --filter @finvesting/core test` | 62파일 **231** 통과 |
| web build | `pnpm --filter @finvesting/web build` | 성공 |
| tax entries | data-tables 집계 `console.info` | tax entries만(realty entries 없음) |

## 정적 검토
| 항목 | 판단 |
|---|---|
| tax A / realty B | payroll 등은 `requireValue`; realty는 최상위 + `assertDefaults` |
| `as any` | `packages/core/src` 0건 |
| 화면 출처 한 줄 | TaxRateGuide·LaborForm·CitForm·profile payroll |

## 검증 못 함 / 다음 AI에게
- 통계·세율 원문 대조(전원 미검증 유지)
- 브라우저에서 「기준 · 출처」 시각 확인
- `dev` merge (사용자 지시 대기)

## 교차 검증
- 09-25 Claude 리뷰 #1~#8 반영 열 갱신
