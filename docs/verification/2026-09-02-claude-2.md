# 2026-09-02 — Claude (Cowork, Cursor 리뷰 검토 세션)
- 환경: 맥북 연결 리눅스 VM (Node 22.23.2). `node_modules`는 Cursor가 맥에서 설치한 것 — `tsc`(순수 JS)는 실행 가능, `vitest`(esbuild 맥 바이너리)는 실행 불가
- 범위: Cursor 변경분 검토, 보류 항목 처리

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| 타입체크 7패키지 (core, interop, db, ai, api, worker, mobile) | `node node_modules/typescript/bin/tsc --noEmit -p` 패키지별 | 전부 exit 0 — Cursor 결과 재확인. web은 Next 플러그인 필요해 제외 |
| 수정 후 core·mobile·worker 타입체크 | 동일 | 통과 (fxRate 포지션, app.config.ts, WORKER_RUN_ON_START 반영 후) |
| git 커밋 | `git commit` | `6f42a51`, lock 파일 정리 |

## 정적 검토 (실행 없이 코드 읽기)
| 항목 | 판단 |
|---|---|
| Cursor 수정 29개 파일 diff | 전부 타당. 특히 월 말일·drizzle-orm 의존·루트 .env 로딩은 실행 즉시 터졌을 버그 |
| `new Date(y, m, 0)` 말일 계산 | m이 1-based 월이므로 정확 |
| progress 로그 "Next `loadEnvConfig`" 문구 | 실제 코드는 직접 파싱 — 문서 불일치, 수정 |
| `app.config.ts`가 app.json과 공존 | Expo가 app.json → app.config 순으로 병합하므로 문제없음 (**Expo 기동으로 확인 필요**) |
| yahoo `today` KST | 미국 장 마감 데이터가 KST 다음날로 저장될 수 있음 — 첫 실행 후 `regularMarketTime` 기준으로 바꿀지 결정 |

## 추가 검증됨 — 사용자가 맥 터미널에서 실행, 출력 확인 (feat/first-run)
| 항목 | 방법 | 결과 |
|---|---|---|
| Docker Postgres 기동 | `open -a Docker` → `pnpm db:up` | 컨테이너 `finvesting-db` 실행 |
| pgvector 확장 | `seed/000_extensions.sql` | `CREATE EXTENSION` |
| `pnpm db:generate` | 맥 | 16 테이블, `drizzle/0000_blue_carlie_cooper.sql` 생성 — `.js` import 제거 후 |
| `pnpm db:migrate` | 맥 | 적용 성공 — `.env` 생성 후 |
| `seed/001_default_user.sql` | 맥 | `INSERT 0 1` × 2 |

## 검증 못 함 / 다음 AI에게
- **새로 추가한 core 포트폴리오 테스트 3개(fxRate) 미실행** — 맥에서 `pnpm -r test` 필요
- `app.config.ts`의 루트 .env 로드가 실제 Expo CLI에서 동작하는지 미확인
- DB·web·worker·Ollama 실행은 여전히 아무도 안 함

## 교차 검증
- Cursor의 "8패키지 typecheck 통과"를 7패키지 재실행으로 확인
- Cursor 리뷰 #1~#23 판단을 diff로 재검토, 이견 없음
