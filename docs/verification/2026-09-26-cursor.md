# 2026-09-26 — Cursor (Composer)

- 환경: macOS, Node 26, pnpm 9.15 (`COREPACK_ENABLE_STRICT=0`)
- 범위: `feat/erp-tax-desk`→`dev` merge, ADR 0050 동결·보류, tax 3파일 대조 준비표

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| `dev` merge·push | `git merge --no-ff feat/erp-tax-desk` + `git push origin dev` | `82538be` |
| 내비 링크 | `layout.tsx` 정적 확인 | `/realty`·`/erp` 없음 |

## 정적 검토
| 항목 | 판단 |
|---|---|
| 라우트 유지 | `apps/web/src/app/realty`·`erp` 디렉터리 삭제 없음 |
| 로드맵 | ERP `[x]` → 보류 절. 부동산 동결 절 |
| 대조 준비 | `2026-09-26-tax-verify-prep.md` — 값 미변경 |

## 검증 못 함
- 브라우저에서 내비 클릭 재확인
- tax 원문 수치 대조(사용자)

## 교차 검증
- 없음
