# 2026-09-12 — Cursor Grok (프로필대장)
- 환경: 맥북 darwin 25.6.0, 기존 `pnpm dev:web` (Next 15.5.25, localhost:3000), Docker Postgres 기동 중
- 범위: `/profile` 프로필대장 UI + `profile.workspaceSave` / `previewPay`

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| api·web typecheck | `pnpm --filter @finvesting/api typecheck` · `pnpm --filter @finvesting/web typecheck` | 통과 |
| `/profile` 렌더 | `GET /profile` · `GET /profile?menu=fixed` | 200. HTML에 `erp`, `프로필대장` |
| `profile.get` | tRPC GET | 세전 4,200,000 · 세후 3,810,000 · 국세 250,000 |
| `profile.previewPay` | tRPC GET 세전 4,200,000 · 국세 250,000 | 국민연금 199,500 · 건보 150,990 · 장기요양 19,840 · 고용 37,800 · 지방세 25,000 · 공제 683,130 · 실수령 3,516,870 (세후 입력 없을 때) |
| `profile.workspaceSave` | 고정비 `대장검증` 1,000 추가 후 같은 API로 삭제 | insert 200 · delete 200. 목록은 휴대폰·전기요금·가스요금만 남음 |
| `profile.statement` | tRPC GET month=2026-09 | recurringSum 121,000, 세액 7·8·9월 유지 |

## 정적 검토 (실행 없이 코드 읽기)
| 항목 | 판단 |
|---|---|
| 툴바 조회/저장/신규/삭제 · 좌측 5메뉴 · 그리드 인라인 수정 | 코드에 있음. 클릭·포커스 이동은 미실행 |
| 홈 `/`는 자산 대시보드, `/profile`만 대장 화면 | 레이아웃 네비 `프로필대장`, 대시보드 링크 `?menu=fixed` |

## 검증 못 함 / 다음 AI에게
- Chrome에서 셀 수정 → 저장 → 미저장 표시 → 조회 되돌리기
- 다크모드·좁은 화면 메뉴 줄바꿈
- 더존·위하고 전표 엑셀 양식 내보내기 (템플릿 없음)

## 교차 검증
- 없음
