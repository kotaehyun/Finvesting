# 2026-09-14 — Cursor Grok (홈·투자 대시보드)
- 환경: 맥북 darwin 25.6.0, Node 26.7.0, Docker Postgres `finvesting-db`, `pnpm --filter @finvesting/web dev` → `127.0.0.1:3000`
- 범위: 웹 타입 분리, 이체·잔액·체결 계산, 홈·투자 화면 개편

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| core 테스트 | `pnpm --filter @finvesting/core test` | 99개 통과 (이체·잔액 선택·매도 검증·통화/평가손익 기여 포함) |
| interop 테스트 | `pnpm --filter @finvesting/interop test` | 11개 통과 (빈 잔액 undefined) |
| 전체 typecheck | `pnpm typecheck` | 통과. 웹 JSX 오류 해소 |
| web 빌드 | `pnpm --filter @finvesting/web build` | 성공. `/` 3.32kB, `/invest` 3.6kB |
| 홈 `/` | 브라우저 데스크톱 | 조회 월·기준일·가져오기. 순자산 3,749,566. 소비 65,000. 저축·투자 실행 0(거래 있음). 평가손익 6,599. 현금흐름 안내. 뉴스 3. 미분류 1건. AI 버튼 |
| 홈 과거 월 | 조회 월 2026-08 | 현재 순자산 유지 + 「8월 순자산은 표시하지 않습니다」. 소비·저축 「이 달 거래 없음」(0원 아님) |
| 홈 모바일 너비 | CDP 390px | 카드 세로 스택, 내비 줄바꿈. 현금흐름 앞자리 잘림 → CSS grid로 수정. 수정 후 재촬영은 탭이 다른 화면으로 옮겨져 못 함 |
| 투자 `/invest` | 브라우저 + tRPC | 지수 10종(코스피 6,684.37). 보유 BTC 평가손익. 관심 없음 안내. 히트맵. 탭 외부 참고·상세 차트 |
| 챗 `?q=` | 브라우저 | 입력창에 재무 상태 질문 미리 채워짐. Ollama 전송은 안 함 |
| 계좌 목록 | 브라우저 | 테스트 입출금·UI검증·검증 코인 표시 |

## 정적 검토 (실행 없이 코드 읽기)
| 항목 | 판단 |
|---|---|
| workspaceSave/commitImport 트랜잭션 | drizzle `db.transaction`으로 묶음. 중간 실패 롤백은 DB 통합 시험 없음 |
| 관심 종목 추가 UI | 조회만. 테이블에 행이 없으면 빈 안내 |
| 6개월 순자산 그래프 | 월말 스냅샷 없어 구현하지 않음 |

## 검증 못 함 / 다음 AI에게
- 이체 분류를 UI에서 저장한 뒤 overview 숫자가 빠지는지(브라우저 클릭)
- 과거 파일 가져오기로 현재 잔액이 안 덮이는지(실파일, 실데이터 변경 금지라 안 함)
- 매수 이전 날짜 매도 API 거부(단위 테스트만)
- Ollama 응답, Expo, ECOS·FRED·DART
- 홈 빈 계정 온보딩(현재 DB에 계좌 있음)
- `dev:lan` 0.0.0.0 실제 LAN 접근

## 교차 검증
- Codex 2026-09-14 P1 타입·이체·잔액·트랜잭션·체결을 코드로 재현한 뒤 수정. 중복 키·USD 합산·Yahoo 뉴욕 날짜(P2)는 그대로
