# 2026-09-02 — Cursor Grok (first-run 코드 리뷰)
- 환경: 맥북 darwin, 저장소 `/Users/th/개발/workspace/Finvesting`, 브랜치 `docs/first-run-review` (`dev`에서 분기). Node는 이 세션에서 `--version` 미확인
- 범위: feat/first-run 머지 이후 `dev` 코드 정적 리뷰 + core/interop 테스트

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| core vitest | `pnpm --filter @finvesting/core test` | 9 passed (cashflow 2, allocation 2, portfolio 5 포함). 이전 “fx 테스트 미실행” 해소 |
| interop vitest | `pnpm --filter @finvesting/interop test` | 3 passed |

## 정적 검토 (실행 없이 코드 읽기)
| 항목 | 판단 |
|---|---|
| worker RSS/Upbit/Yahoo 실패 격리, env 경로 4단계 | 코드와 logs·data-sources.md 일치 |
| 대시보드 월 말일 `new Date(y, m, 0)` | m이 1-based라 말일 계산 맞음 |
| Yahoo 시세일 `regularMarketTime` NY | 스캐폴딩 리뷰 #8 후속. 펀더멘털 날짜는 여전히 KST today |
| chat IME `isComposing` | 의도대로 보임 (사용자 실행 확인은 기존 verification) |
| Next `.env` 인라인 주석 | 커스텀 파서가 `#`를 값에 포함. 실제 `.env`의 `OLLAMA_MODEL`은 주석 없음. 빈 키+주석 줄은 Next에서 truthy가 될 수 있음 |
| `market.latestNews` 전 컬럼 | embedding/raw 포함. 지금은 embedding null |

## 검증 못 함 / 다음 AI에게
- 이 세션에서 `pnpm typecheck`·`pnpm dev:web`·worker·Ollama는 실행하지 않음
- 모바일 Expo 기동 여전히 ❌
- ECOS·FRED·DART·EDGAR는 키/대상 없어 보류 유지
- 리뷰 항목 수정 반영 여부 — 사용자 지시 전 미착수

## 교차 검증
- Claude가 기록한 “fx 테스트 3개 미실행”을 맥에서 재실행해 통과 확인
- 스캐폴딩 리뷰 1–19, 21–23은 현재 코드에서 재발하지 않음
