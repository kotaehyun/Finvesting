# 2026-09-05 — Cursor Grok (리뷰 반영 + 실행)
- 환경: 맥북, Node 26.7.0, 브랜치 `fix/first-run-review`. Docker Desktop 기동 후 `pnpm db:up`, `pnpm dev:web`(Next 15.5.25)
- 범위: first-run 리뷰 반영분 런타임 확인

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| api/worker/ai typecheck | `pnpm --filter … typecheck` | 통과 |
| Postgres | `pnpm db:up` 후 `pg_isready` | 컨테이너 `finvesting-db` ready |
| `GET /` | curl | 200 |
| `GET /chat` | curl | 200, HTML에 “투자 비서” |
| tRPC `dashboard.overview` + `market.latestNews` | 대시보드와 같은 배치 GET | 200. month=2026-09, accountCount=0. 뉴스 키는 `id,title,url,publisher,publishedAt,summary`만 — embedding/raw 없음 |
| `chat.ask` 보유·현금흐름 | POST, gemma4:12b 25s | 200. “자산 현황, 현금흐름, 보유 종목 데이터가 아직 없습니다.” (계좌 0건과 일치). 거시지표는 DB 0건이라 모델도 없다고 답함 |
| `chat.ask` 최근 뉴스 3개 | POST 29s | 200. 연합·매경 제목을 DB/latestNews와 맞게 인용 |
| worker `run:once` 1차 | 맥 | rss 신규 268 (8/8 피드), upbit 4, yahoo 4/4. ECOS/FRED/DART skip(키 없음). **EDGAR skip `SEC_USER_AGENT not set`** (신규 동작) |
| worker `run:once` 2차 (같은 날 upsert) | 맥 | rss 0/전체. upbit 4 — `2026-09-05` 행의 `fetched_at` 갱신, BTC close 108850000→108852000, open/high/low 유지. yahoo 4 — 시세일 NY `2026-09-04`, OHLC 있음, `fetched_at` 갱신 |

## 정적 검토 (실행 없이 코드 읽기)
| 항목 | 판단 |
|---|---|
| Next `.env` 인라인 주석 | 실제 `.env`의 `OLLAMA_MODEL`은 주석 없음. 챗이 모델명으로 응답한 것으로 로딩은 됨 |

## 검증 못 함 / 다음 AI에게
- 브라우저 자동화 도구 없음 — curl·tRPC·psql로 대체. 화면 클릭/IME는 미확인
- ECOS·FRED·DART는 키 없어 skip만 확인
- `pnpm --filter @finvesting/web typecheck`는 `ReactMarkdown` React 19 JSX 타입 오류 (기존)

## 교차 검증
- 2026-09-02 사용자 실행과 동일 경로(`/`, tRPC 배치, `/chat`+Ollama, worker RSS/Upbit/Yahoo)를 재실행해 통과
