# 2026-09-13 — Cursor Grok (시세 대상·USDKRW·투자자산)
- 환경: 맥북, Node/pnpm. Docker 데몬 꺼짐 (`docker.sock` 없음)
- 범위: 검토 1~4 반영 (`feat/quote-targets`)

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| typecheck 8패키지 | `pnpm typecheck` | 통과 |
| core 테스트 | vitest | 48개 통과 (quote-targets 8 · invested 3) |
| interop 테스트 | vitest | 9개 통과 |
| `feat/financial-profile` 커밋 | `git log` | 6커밋 `696d957`~`a777272`. `_to_delete/` 제외 |

## 정적 검토
| 항목 | 판단 |
|---|---|
| `latestUsdKrw` | `code=USDKRW` 최신 1줄. 소스 필터 없음 |
| ECOS 우선 | 같은 날짜 yahoo는 건너뛰고, ECOS upsert가 `source=ecos`로 덮어씀 |

## 검증 못 함 / 다음 AI에게
- `pnpm --filter @finvesting/worker run:once` — Docker 꺼짐
- `market_quotes`에 보유 BTC가 들어갔는지
- `macro_indicators` USDKRW · `trades.holdings`의 `usdkrw`/`pnlKrw`
- 대시보드 브라우저에서 「평가+예수금」 문구

## 교차 검증
- 없음
