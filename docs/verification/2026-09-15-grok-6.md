# 2026-09-15 — Grok (6)

- 환경: macOS Darwin 25.6.0, Node, pnpm 9.15.0, Chrome (Cursor 브라우저)
- 범위: `/realty` MapLibre 3D 도면 + 시·구 선택 시 시도 가계대출 그래프

## 검증됨 (실제 실행)
| 항목 | 방법 | 결과 |
|---|---|---|
| core 테스트 | `pnpm --filter @finvesting/core test` | 31파일 122개 통과 (realty-loans 2개 포함) |
| web/api/worker 타입 | `tsc --noEmit` | api·worker·web 통과. core typecheck는 기존 `world-indices.test.ts` TS2532로 실패(이번 변경 아님) |
| `/realty` HTTP | 개발 서버 GET | 200. tRPC `market.newsFeed`+`market.realtyLoans` 200 |
| 3D 도면 | Chrome | 워커 URL 고정 후 수도권 면 보임. 서울 주황 기둥, 인천 파랑, 동탄 빨간 원. 서울 칩→종로구 팝업·확대 |
| 대출 패널 | Chrome 서울 25구 클릭 | 「서울 25구 전역 · 서울 예금은행」. 시·구 숫자 없음 문구. 시계열은 worker skip이라 「이 시도 시계열이 없습니다」 |
| worker ECOS 대출 | `collectEcos` | `ECOS_API_KEY not set` skip. 지역 항목 매칭·말잔 upsert는 미실행 |

## 정적 검토 (실행 없이 코드 읽기)
| 항목 | 판단 |
| 시·구 대출액을 지어내지 않음 | `realtyMetroForPlace`로 광역시도만. 높이는 시도 비율 |
| 지역 ITEM_CODE 추측 없음 | `StatisticItemList` 이름 매칭 실패 시 skip |

## 검증 못 함 / 다음 AI에게
- ECOS 키로 `151Y003` 서울·경기·인천 말잔이 실제로 들어오는지, 전년동월 그래프 숫자
- 모바일 뷰포트 3D 조작
- 면 클릭(칩이 아니라 폴리곤) 좌표 — 칩·팝업은 확인

## 교차 검증
- 없음
