# 0003 — 수집기는 별도 worker 앱
- 상태: 채택 (2026-09-02)
- 배경: 뉴스·시세·지표를 주기적으로 수집해야 함. Next.js API 라우트는 요청 단위 실행이라 장시간·예약 작업에 부적합.
- 결정: `apps/worker`에 node-cron 스케줄러 + 소스별 어댑터. DB에만 쓰고 API를 거치지 않음.
- 결과: 로컬에선 `pnpm dev`로 같이 실행, 서비스화 시 컨테이너 1개로 분리 배포.
