# 세션 종료 프롬프트

---

작업을 마무리합니다. 아래를 순서대로 수행하고 결과를 보고하세요.

1. `pnpm typecheck`가 통과하는지 확인 (실패하면 고치거나, 못 고치면 막힌 것에 기록)
2. `docs/progress/README.md` **맨 위**에 오늘 항목 추가:
   - 날짜 — 제목
   - 한 일 (구체적으로, 파일/기능 단위)
   - 다음 할 일 (번호 목록, 우선순위순)
   - 막힌 것 / 미결정
3. 이번에 바꾼 영역의 `docs/tech/*` 문서가 코드와 일치하는지 확인하고 갱신
4. 기술 결정이 있었으면 `docs/tech/decisions/`에 ADR 추가하고 README 표 갱신
5. 해결한 오류는 `docs/logs/`, 리뷰는 `docs/review/`에 기록했는지 확인
6. 프롬프트가 부족해서 헤맨 부분이 있었으면 `docs/prompts/`의 해당 파일을 고치고 README 변경 이력에 남김
7. 변경 파일을 `type(scope): 요약` 형식으로 커밋 (푸시는 사용자가 지시할 때만)
8. 마지막으로 다음 세션의 AI가 바로 이어받을 수 있도록 **3~5문장 인수인계 요약**을 출력
